# LLM Prompt Node

This node sends a prompt template to OpenAI and outputs the generated response text.

## Configuration

- `promptText`: The template text to send. Supports direct instructions.
- `model`: The model identifier (e.g. `gpt-4o-mini`, `gpt-4o`).

## Requirements

Requires the `OPENAI_API_KEY` environment variable to be set.
