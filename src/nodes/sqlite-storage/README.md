# SQLite Storage Node

This node appends data to an embedded SQLite database table.

## Configuration

- `tableName`: Destination table name (alphanumeric and underscores only).
- `columnName`: Column name in the table for storing data.
